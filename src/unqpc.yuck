Usage: unqpc < LINES

For each line of LINES unquote percent encoding, optionally recursively.

  --only-printable      Only decode sequences that pass isprint(3).
  -r, --recursive       Unquote recursively.
